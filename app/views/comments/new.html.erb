<%= form_for @comment, url: {action: "new"} do |f| %>
  <fieldset>
    <legend>Comment</legend>
    <%= f.text_area :comment, size: "60x12" %>
    <br>
    <%= f.submit "Create" %>
  </fieldset>
<% end %>
