<%= form_for @post, url: {action: "new"} do |f| %>
  <fieldset>
    <legend>Post</legend>
    <%= label_tag 'title' %>
    <%= f.text_field :title %>
    <br>
    <%= label_tag 'body' %>
    <%= f.text_area :text, size: "60x12" %>
    <br>
    <%= f.submit "Create" %>
  </fieldset>
<% end %>
